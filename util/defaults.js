import defaultData from './default.json' with { type: 'json' };

export const {
	limit: botLimits,
	defaultPermissions,
	defaultSettings,
	timeoptions,
	usergroups,
	defaultNamespaces
} = defaultData;